# Backend

ASP.NET Core 9 solution following Clean Architecture and CQRS (MediatR,
FluentValidation, Entity Framework Core, PostgreSQL, Redis).

Not yet scaffolded — the solution and projects
(`Domain`, `Application`, `Infrastructure`, `API`) are created in Phase 1
(Architecture), where we design the layer boundaries and dependency rules
before generating any project files.
