Cross-feature React hooks (e.g. `useDebounce`, `useMediaQuery`) — hooks used
by more than one feature. Feature-specific hooks live in
`features/{feature}/hooks/` instead. Empty until a second feature needs to
share one.
