# Infrastructure

Docker Compose for local development (PostgreSQL, Redis, backend, frontend)
and, later, Terraform for Azure provisioning.

Docker Compose is introduced in Phase 3 (Database). Terraform is introduced
in Phase 9 (Deployment), deliberately deferred — infrastructure-as-code for
resources that don't exist yet is speculative work.
