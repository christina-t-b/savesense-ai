# Scripts

Local developer and CI helper scripts (e.g. spin up the full stack, seed the
database, run all test suites across frontend/backend).

Added as they become necessary — starting with a stack bootstrap script in
Phase 3.
