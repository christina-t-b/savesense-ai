# Shared

Cross-cutting contracts consumed by more than one app — e.g. TypeScript
types generated from the backend's OpenAPI spec, shared constants/enums used
by both the frontend and the Chrome extension.

Populated starting in Phase 2, once the backend has endpoints to generate
contracts from. Kept intentionally empty until then to avoid speculative
abstractions.
